"""
In the Terminal use the command python3 -m pytest to run the tests
Print a dot for each passing test case
Conclusion with a test summary.

"""
def test_one_plus_one():
    assert 1 + 1 == 2


def test_one_plus_two():
    a =1
    b = 2
    c = 0
    assert a + b == c

# Topic went over the details in the console you see such as failed tests.